version: '3.8'

services:
  # Init container to set proper ownership
  volume-init:
    image: alpine:latest
    command: |
      sh -c "
        chown -R 1001:1001 /auth-data /user-data /chat-data /game-data /user-uploads &&
        chmod -R 755 /auth-data /user-data /chat-data /game-data /user-uploads
      "
    volumes:
      - auth-db:/auth-data
      - user-db:/user-data
      - chat-db:/chat-data
      - game-db:/game-data
      - user-uploads:/user-uploads
    networks:
      - pong-network

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    depends_on:
      - gateway-service
      - chat-service
    networks:
      - pong-network
    volumes:
      - ./certs:/etc/nginx/ssl:ro

  auth-service:
    build:
      context: ./services/auth-service
      dockerfile: Dockerfile
    ports:
      - "3001:3001"
    env_file:
      - ./services/auth-service/.env
    environment:
      - NODE_ENV=production
      - PORT=3001
      - SSL_CERT=/app/ssl/cert.pem
      - SSL_KEY=/app/ssl/key.pem
      - NODE_TLS_REJECT_UNAUTHORIZED=0
    volumes:
      - auth-db:/app/data
      - ./certs:/app/ssl:ro
    user: "1001:1001"
    depends_on:
      - volume-init
    networks:
      - pong-network

  user-service:
    build:
      context: ./services/user-service
      dockerfile: Dockerfile
    ports:
      - "3002:3002"
    env_file:
      - ./services/user-service/.env
    environment:
      - NODE_ENV=production
      - PORT=3002
      - SSL_CERT=/app/ssl/cert.pem
      - SSL_KEY=/app/ssl/key.pem
      - NODE_TLS_REJECT_UNAUTHORIZED=0
    volumes:
      - user-db:/app/data
      - user-uploads:/app/uploads
      - ./certs:/app/ssl:ro
    user: "1001:1001"
    depends_on:
      - volume-init
    networks:
      - pong-network

  chat-service:
    build:
      context: ./services/chat-service
      dockerfile: Dockerfile
    ports:
      - "3003:3003"
    env_file:
      - ./services/chat-service/.env
    environment:
      - NODE_ENV=production
      - PORT=3003
      - SSL_CERT=/app/ssl/cert.pem
      - SSL_KEY=/app/ssl/key.pem
      - NODE_TLS_REJECT_UNAUTHORIZED=0
    volumes:
      - chat-db:/app/data
      - ./certs:/app/ssl:ro
    user: "1001:1001"
    depends_on:
      - volume-init
    networks:
      - pong-network

  game-service:
    build:
      context: ./services/game-service
      dockerfile: Dockerfile
    ports:
      - "3004:3004"
    env_file:
      - ./services/game-service/.env
    environment:
      - NODE_ENV=production
      - PORT=3004
      - SSL_CERT=/app/ssl/cert.pem
      - SSL_KEY=/app/ssl/key.pem
      - NODE_TLS_REJECT_UNAUTHORIZED=0
    volumes:
      - game-db:/app/data
      - ./certs:/app/ssl:ro
    user: "1001:1001"
    depends_on:
      - volume-init
    networks:
      - pong-network

  gateway-service:
    build:
      context: ./services/gateway-service
      dockerfile: Dockerfile
    ports:
      - "3005:3005"
    env_file:
      - ./services/gateway-service/.env
    environment:
      - NODE_ENV=production
      - PORT=3005
      - SSL_CERT=/app/ssl/cert.pem
      - SSL_KEY=/app/ssl/key.pem
      - NODE_TLS_REJECT_UNAUTHORIZED=0
      - AUTH_SERVICE_URL=https://auth-service:3001
      - USER_SERVICE_URL=https://user-service:3002
      - CHAT_SERVICE_URL=https://chat-service:3003
      - GAME_SERVICE_URL=https://game-service:3004
    depends_on:
      - auth-service
      - user-service
      - chat-service
      - game-service
    networks:
      - pong-network
    volumes:
      - ./certs:/app/ssl:ro

networks:
  pong-network:
    driver: bridge

volumes:
  auth-db:
  user-db:
  user-uploads:
  chat-db:
  game-db: